# 🚀 Быстрый старт 77-8DOC

## Для нетерпеливых

```bash
1. Запустите:        start.bat
2. Настройте .env:   (откроется автоматически)
3. Откройте браузер: http://localhost:8100
```

## Что это?

Веб-приложение для просмотра документов из 1С 7.7:
- 📄 Реализация товаров и услуг
- 🧾 Счета-фактуры выданные

## Системные требования

- ✅ Windows (для COM)
- ✅ Python 3.11+
- ✅ 1С 7.7 (зарегистрированный COM-сервер)
- ✅ Доступ к базе 1С 7.7

## Первый запуск

### Шаг 1: Запустите start.bat
Двойной клик на `start.bat` - скрипт всё сделает сам.

### Шаг 2: Настройте .env
Откроется блокнот с файлом .env. Укажите:
```ini
DB_PATH_77=\\сервер\путь\к\базе
DB_USER_77=Администратор
DB_PASSWORD_77=пароль
```

### Шаг 3: Зарегистрируйте COM (если нужно)
Если start.bat сообщит об ошибке COM, выполните от администратора:
```bash
"C:\Program Files\1Cv77\BIN\1cv7s.exe" /REGSERVER
```

### Шаг 4: Откройте браузер
```
http://localhost:8100
```

## Использование

1. **Выберите период** (дата начала и конца)
2. **Выберите тип документа** (Реализация или Счет-фактура)
3. **Нажмите "Загрузить"**
4. **Просмотрите документы** в таблице
5. **Кликните на 👁** для просмотра табличной части

## Полезные команды

| Команда | Описание |
|---------|----------|
| `start.bat` | Запустить приложение |
| `test_connection.bat` | Проверить подключение к 1С |
| `stop.bat` | Остановить сервер |

## Проблемы?

### ❌ Ошибка подключения к 1С
```bash
test_connection.bat  # Проверьте подключение
```

### ❌ Порт 8100 занят
Измените в .env:
```ini
PORT=8101
```

### ❌ COM-сервер не найден
От администратора:
```bash
"C:\Program Files\1Cv77\BIN\1cv7s.exe" /REGSERVER
```

## Логи

Смотрите в `logs/app.log`

## API (для разработчиков)

```bash
# Health check
curl http://localhost:8100/api/v1/health

# Получить документы
curl -X POST http://localhost:8100/api/v1/realizations \
  -H "Content-Type: application/json" \
  -d "{\"start_date\": \"01.05.2026\", \"end_date\": \"10.05.2026\"}"
```

## Структура проекта

```
77-8DOC/
├── start.bat              ← Запуск
├── test_connection.bat    ← Проверка
├── stop.bat               ← Остановка
├── .env                   ← Настройки
├── backend/               ← Python код
├── frontend/              ← HTML/JS/CSS
└── logs/                  ← Логи
```

## Следующие шаги

- [ ] Протестировать на сервере
- [ ] Добавить интеграцию с 1С 8 (OData)
- [ ] Добавить экспорт в Excel
- [ ] Добавить фильтры по контрагентам

## Поддержка

📧 Вопросы? Смотрите:
- `README.md` - полная документация
- `BAT_README.md` - подробно про bat-файлы
- `INSTALL.md` - детальная инструкция
- `logs/app.log` - логи приложения

---

**Версия:** 1.0.0  
**Дата:** 2026-05-10
